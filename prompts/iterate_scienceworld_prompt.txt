You are continuing the cognitive-agent iteration workflow in {repo_root}.

Random ScienceWorld experiment to analyze: {experiment_id}
Current stacked base branch: {current_branch}
Suggested next agent-iteration number: {next_iteration}

Experiment summary JSON:
{experiment_summary_json}

Workflow:
1. Analyze experiment {experiment_id} using local run artifacts and DB metadata when available.
2. Plan the best improvement before editing code.
3. Implement only the best improvement, with token-efficiency, task generalization, and environment agnosticism as top priorities.
4. Update AGENTS.md if the new iteration changes the documented stack or workflow expectations.
5. Run the relevant tests for the files you touched.
6. If the best change is a meaningful agent-behavior iteration, create a new stacked branch named `agent-iter-{next_iteration}-<topic>` from `{current_branch}`, commit the work, push it, and open a PR against `{current_branch}`.
7. If the best change is cross-cutting and belongs on `main` instead of the agent stack, do not create an agent-iteration PR. Stop after explaining that recommendation clearly.
8. Do not touch unrelated local changes already present in the worktree.
