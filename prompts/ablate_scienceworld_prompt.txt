You are continuing the cognitive-agent consolidation workflow in {repo_root}.

Regression anchor experiment to analyze: {experiment_id}
Current stacked base branch: {current_branch}
Suggested next agent-iteration number: {next_iteration}

Experiment summary JSON:
{experiment_summary_json}

Workflow:
1. Analyze experiment {experiment_id} using local run artifacts and DB metadata when available.
2. Identify one recent heuristic cluster, prompt/context expansion, or scoring path that is overlapping, brittle, or unnecessarily expensive.
3. Plan the best simplification or ablation before editing code.
4. Implement only the best simplification, with token-efficiency, task generalization, and environment agnosticism as top priorities.
5. Prefer deleting redundant logic, merging overlapping runtime structures, or shrinking model-facing context over adding a new heuristic.
6. Update AGENTS.md if the new consolidation changes the documented workflow or stack expectations.
7. Run the relevant tests for the files you touched.
8. If the simplification is worthwhile and belongs in the stack, create a new stacked branch named `agent-iter-{next_iteration}-consolidate-<topic>` from `{current_branch}`, commit the work, push it, and open a PR against `{current_branch}`.
9. If the best conclusion is that the recent logic should stay as-is, stop after explaining that clearly and do not open a PR.
10. Do not touch unrelated local changes already present in the worktree.
